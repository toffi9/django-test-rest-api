sudo: required
language: python

services:
  - docker

env:
  # TODO: installing newest version of docker-compose always
  - DOCKER_COMPOSE_VERSION=1.16.1

before_install:
  # Installing newest docker-ce
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
  - sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
  - sudo apt-get update
  - sudo apt-get -y install docker-ce
  - docker -v
  # Installing docker-compose
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  - docker-compose -v

install:
  - docker-compose -f dev.yml build

script:
  # linting whole project (api, tests)
  - docker-compose -f dev.yml run --rm api flake8
  # unit tests
  - docker-compose -f dev.yml run --rm api pytest -vv

# TODO: pushing to ECR when master or develop is ok
# after_success:
#   - if [ "$TRAVIS_BRANCH" == "master" ]; then
#     docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
#     docker push USER/REPO;
#     fi
